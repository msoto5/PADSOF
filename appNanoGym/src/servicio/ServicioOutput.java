package servicio;
/*
import java.io.*;

public class ServicioOutput {
    private FileOutputStream file;
    private ObjectOutputStream output;

    // Abrir el fichero
    public void abrir()
            throws IOException {
        file = new FileOutputStream("./src/backUp/servicios.ser");
        output = new ObjectOutputStream(file);
    }

    // Cerrar el fichero
    public void cerrar()
            throws IOException {
        if (output != null)
            output.close();
    }

    // Escribir en el fichero
    public void escribir(Servicio servicio)
            throws IOException {
        if (output != null)
            output.writeObject(servicio);
    }

}
*/