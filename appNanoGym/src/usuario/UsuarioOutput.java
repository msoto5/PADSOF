package usuario;
/*
import java.io.*;

public class UsuarioOutput {
    private FileOutputStream file;
    private ObjectOutputStream output;

    // Abrir el fichero
    public void abrir()
            throws IOException {
        file = new FileOutputStream("./src/backUp/usuarios.ser");
        output = new ObjectOutputStream(file);
    }

    // Cerrar el fichero
    public void cerrar()
            throws IOException {
        if (output != null)
            output.close();
    }

    // Escribir en el fichero
    public void escribir(Usuario usuario)
            throws IOException {
        if (output != null)
            output.writeObject(usuario);
    }
 
}
*/