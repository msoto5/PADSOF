package sala;
/*
import java.io.*;

public class SalaOutput {
    private FileOutputStream file;
    private ObjectOutputStream output;

    // Abrir el fichero
    public void abrir()
            throws IOException {
        file = new FileOutputStream("./src/backUp/salas.ser");
        output = new ObjectOutputStream(file);
    }

    // Cerrar el fichero
    public void cerrar()
            throws IOException {
        if (output != null)
            output.close();
    }

    // Escribir en el fichero
    public void escribir(Sala sala)
            throws IOException {
        if (output != null)
            output.writeObject(sala);
    }

}
*/
